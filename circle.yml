#
# Build configuration for Circle CI
#

machine:
    java:
        version: openjdk8

dependencies:
    override:
        - ./gradlew dependencies

test:
    override:
        - (./gradlew build test):
            timeout: 360